<div class="gummy-features-section gummy-{{ section.id }}">
  <div class="gummy-container">
    {%- if section.settings.title != blank -%}
      <h2 class="gummy-main-title">{{ section.settings.title }}</h2>
    {%- endif -%}

    <div class="gummy-grid">
      {%- for block in section.blocks -%}
        <div class="gummy-card" {{ block.shopify_attributes }}>
          <div class="gummy-icon-wrapper">
            {%- if block.settings.image != blank -%}
              <img 
                src="{{ block.settings.image | image_url: width: 200 }}" 
                alt="{{ block.settings.title | escape }}"
                loading="lazy"
                width="auto"
                height="auto"
              >
            {%- else -%}
              {{ 'image' | placeholder_svg_tag: 'gummy-placeholder' }}
            {%- endif -%}
          </div>
          
          <h3 class="gummy-card-title">{{ block.settings.title }}</h3>
          
          <div class="gummy-card-text">
            {{ block.settings.text }}
          </div>
        </div>
      {%- endfor -%}
    </div>
  </div>
</div>

{% style %}
.gummy-{{ section.id }} {
  background-color: {{ section.settings.bg_color }};
  padding-top: {{ section.settings.padding_top }}px;
  padding-bottom: {{ section.settings.padding_bottom }}px;
}

/* Upscaled layout boundary from 1200px to 1600px */
.gummy-container {
  max-width: 1600px;
  margin: 0 auto;
  padding: 0 40px;
}

/* Bigger Section Heading */
.gummy-main-title {
  font-size: 36px;
  font-weight: 700;
  color: #1a1a1a;
  margin-top: 0;
  margin-bottom: 40px;
  font-family: inherit;
  position: relative;
}

/* Section divider line */
.gummy-main-title::after {
  content: '';
  display: block;
  width: 100%;
  height: 1px;
  background-color: #e0e0e0;
  margin-top: 20px;
}

/* Spacing between large blocks */
.gummy-grid {
  display: grid;
  grid-template-columns: repeat(6, 1fr);
  gap: 24px;
}

/* Deep padding and rounded styling to make cards visually larger */
.gummy-card {
  background-color: #f5f5f5;
  border-radius: 16px;
  padding: 45px 25px;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  box-sizing: border-box;
}

/* Upscaled logo containers */
.gummy-icon-wrapper {
  height: 90px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 25px;
  width: 100%;
}

.gummy-icon-wrapper img {
  max-height: 100%;
  max-width: 100%;
  object-fit: contain;
}

.gummy-placeholder {
  width: 70px;
  height: 70px;
  fill: #999;
}

/* Larger card headlines */
.gummy-card-title {
  font-size: 19px;
  font-weight: 700;
  color: #222222;
  margin-top: 0;
  margin-bottom: 16px;
}

/* Scaled paragraph text */
.gummy-card-text {
  font-size: 15px;
  line-height: 1.6;
  color: #505050;
}

.gummy-card-text p {
  margin: 0;
}

/* Responsive Breakpoints */
@media (max-width: 1200px) {
  .gummy-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
}

@media (max-width: 768px) {
  .gummy-container {
    padding: 0 20px;
  }
  .gummy-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 15px;
  }
  .gummy-card {
    padding: 30px 15px;
  }
  .gummy-main-title {
    font-size: 26px;
  }
}
{% endstyle %}

{% schema %}
{
  "name": "Gummy Trust Features",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Delicious Gummy Vitamins"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 20,
      "max": 120,
      "step": 5,
      "unit": "px",
      "label": "Top Padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 20,
      "max": 120,
      "step": 5,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 60
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature Card",
      "limit": 6,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Badge Icon/Logo"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Card Title",
          "default": "Feature Title"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Card Description",
          "default": "<p>Add short descriptive text here matching your badge asset.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Gummy Trust Features",
      "blocks": [
        { "type": "feature", "settings": { "title": "B Corp Certified" } },
        { "type": "feature", "settings": { "title": "Cruelty Free" } },
        { "type": "feature", "settings": { "title": "3rd Party Tested" } },
        { "type": "feature", "settings": { "title": "FDA Registered Facility" } },
        { "type": "feature", "settings": { "title": "GMP Certified" } },
        { "type": "feature", "settings": { "title": "Expert Formulated" } }
      ]
    }
  ]
}
{% endschema %}
