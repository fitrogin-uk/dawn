<div class="gummy-features-section gummy-{{ section.id }}">
  <div class="gummy-container">
    {%- if section.settings.title != blank -%}
      <h2 class="gummy-main-title">{{ section.settings.title }}</h2>
    {%- endif -%}

    <div class="gummy-grid">
      {%- for block in section.blocks -%}
        <div class="gummy-card" {{ block.shopify_attributes }}>
          <div class="gummy-icon-wrapper">
            {%- if block.settings.image != blank -%}
              <img 
                src="{{ block.settings.image | image_url: width: 250 }}" 
                alt="{{ block.settings.title | escape }}"
                loading="lazy"
                width="auto"
                height="auto"
              >
            {%- else -%}
              {{ 'image' | placeholder_svg_tag: 'gummy-placeholder' }}
            {%- endif -%}
          </div>
          
          <h3 class="gummy-card-title">{{ block.settings.title }}</h3>
          
          <div class="gummy-card-text">
            {{ block.settings.text }}
          </div>
        </div>
      {%- endfor -%}
    </div>
  </div>
</div>

{% style %}
.gummy-{{ section.id }} {
  background-color: {{ section.settings.bg_color }};
  padding-top: {{ section.settings.padding_top }}px;
  padding-bottom: {{ section.settings.padding_bottom }}px;
}

/* Wide screen boundary layout */
.gummy-container {
  max-width: 1800px;
  margin: 0 auto;
  padding: 0 50px;
}

/* Section Header - Line repositioned in front (above) the text */
.gummy-main-title {
  font-size: 38px;
  font-weight: 700;
  color: #1a1a1a;
  margin-top: 0;
  margin-bottom: 50px;
  font-family: inherit;
  position: relative;
  padding-top: 25px; /* Adds space between the top line and the text */
}

/* Horizontal line placed BEFORE/IN FRONT of the heading text */
.gummy-main-title::before {
  content: '';
  display: block;
  width: 100%;
  height: 1px;
  background-color: #e0e0e0;
  position: absolute;
  top: 0;
  left: 0;
}

/* Spacing between scaled cards */
.gummy-grid {
  display: grid;
  grid-template-columns: repeat(6, 1fr);
  gap: 32px;
}

/* 
  BOX ADJUSTMENTS COMPLETED:
  - Vertical spacing (Padding top/bottom) increased by 45% (to 65px)
  - Horizontal spacing (Padding left/right) increased by 20% (to 30px)
*/
.gummy-card {
  background-color: #f5f5f5;
  border-radius: 20px;
  padding: 65px 30px;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  box-sizing: border-box;
  width: 100%;
}

/* 45% vertical increase applied to the logo container zone */
.gummy-icon-wrapper {
  height: 130px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 35px;
  width: 100%;
}

.gummy-icon-wrapper img {
  max-height: 100%;
  max-width: 100%;
  object-fit: contain;
}

.gummy-placeholder {
  width: 90px;
  height: 90px;
  fill: #999;
}

/* Scaled card headers */
.gummy-card-title {
  font-size: 21px;
  font-weight: 700;
  color: #222222;
  margin-top: 0;
  margin-bottom: 20px;
}

/* Scaled card body text */
.gummy-card-text {
  font-size: 16px;
  line-height: 1.6;
  color: #4a4a4a;
}

.gummy-card-text p {
  margin: 0;
}

/* Responsive Scaling Breakpoints */
@media (max-width: 1400px) {
  .gummy-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }
}

@media (max-width: 768px) {
  .gummy-container {
    padding: 0 20px;
  }
  .gummy-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 15px;
  }
  .gummy-card {
    padding: 45px 20px;
  }
  .gummy-main-title {
    font-size: 28px;
    padding-top: 20px;
  }
}
{% endstyle %}

{% schema %}
{
  "name": "Gummy Trust Features",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Delicious Gummy Vitamins"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 20,
      "max": 120,
      "step": 5,
      "unit": "px",
      "label": "Top Padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 20,
      "max": 120,
      "step": 5,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 60
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature Card",
      "limit": 6,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Badge Icon/Logo"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Card Title",
          "default": "Feature Title"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Card Description",
          "default": "<p>Add short descriptive text here matching your badge asset.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Gummy Trust Features",
      "blocks": [
        { "type": "feature", "settings": { "title": "B Corp Certified" } },
        { "type": "feature", "settings": { "title": "Cruelty Free" } },
        { "type": "feature", "settings": { "title": "3rd Party Tested" } },
        { "type": "feature", "settings": { "title": "FDA Registered Facility" } },
        { "type": "feature", "settings": { "title": "GMP Certified" } },
        { "type": "feature", "settings": { "title": "Expert Formulated" } }
      ]
    }
  ]
}
{% endschema %}
